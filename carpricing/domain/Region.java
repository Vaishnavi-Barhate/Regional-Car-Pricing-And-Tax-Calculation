package carpricing.domain;

/**
 * Enum representing different regions across the globe.
 * Each region corresponds to a continent.
 */
public enum Region {

    R1, // USA
    R2, // Europe
    R3, // Asia
    R4  // Africa
}
